{
  "name": "trackradar",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "TrackRadar - Cloudflare Worker that polls YouTube channel uploads and stores new videos as JSON in a GitHub repository.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.135.0"
  }
}
